Да 😄 Начинаем с самого простого и важного — **проверяем первый скилл отдельно**, не весь пайплайн сразу.

Создай пустой тестовый проект, например:

```text
Documents/
└── UiKit PDLC Ready/
    └── pdlc-smoke-test/
        └── .gigacode/
            └── product-context.yaml
```

В `product-context.yaml` у тебя уже должно быть:

```yaml
version: 1
context_id: norm
```

Теперь в GigaCode Desktop открой именно папку:

```text
/Users/18868085/Documents/UiKit PDLC Ready/pdlc-smoke-test
```

Сделай:

```text
/clear
```

И первым запросом дай **ровно это**:

```text
Используй skill formulate-product-task.

Контекст продукта: norm.

Хочу сформулировать продуктовую задачу для нового автономного раздела NORM.

Сейчас риск-менеджер может запускать AI-анализ документов вручную или анализ может запускаться автоматически. После этого пользователю неудобно понимать историю запусков: что анализировалось, когда был запуск, завершился ли он успешно и какой результат был получен.

Нужен единый способ посмотреть последние и прошлые запуски анализа.

Пользователю важно:
- видеть дату и время запуска;
- понимать, что стало источником анализа;
- видеть статус: выполняется, завершён, ошибка;
- видеть количество найденных рисков;
- открывать результат завершённого анализа;
- повторно запускать анализ, если предыдущий завершился ошибкой.

Основной пользователь — риск-менеджер.

Для MVP не нужны:
- массовые операции;
- экспорт;
- настройка расписания автоматического анализа;
- изменение результатов анализа;
- сложная матрица прав.

Считай это новой автономной поверхностью, а не изменением существующего экрана.

Сохрани результат в:
01_product_brief.md
```

### Сейчас наша задача — не вмешиваться

Не подсказывай ему структуру документа и не объясняй, что должно быть внутри. Нам нужно понять, сработали ли:

```text
skill
→ context selector
→ global context registry
→ manifest
→ профиль for_product_task
→ генерация brief
```

Когда GigaCode закончит, посмотри на три вещи:

1. Создался ли файл:
   ```text
   01_product_brief.md
   ```

2. Написал ли он где-нибудь, что использовал:
   ```text
   context_id: norm
   ```

3. Не сказал ли он что-то вроде:
   ```text
   context not found
   manifest not found
   BLOCKED_BY_CONTEXT
   ```

Если файл создался — **не переходи пока ко второму скиллу**. Просто пришли мне сюда либо весь `01_product_brief.md`, либо ответ GigaCode целиком.

Я сначала посмотрю: действительно ли первая ступень отработала правильно, а не просто «какой-то MD появился». После этого дадим ему подтверждение и перейдём к UX.
